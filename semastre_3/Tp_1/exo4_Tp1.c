#include <stdio.h>
#include "functions_Tp1.c"

int main(){
    int n,pos,number;
    scanf("%d",&n);
    int table[n];
    registre_table(n,table);
    scanf("%d",&pos);
    scanf("%d",&number);
    int table_[n];
    if (pos>n){
        
    }
    add_element(n,table_,pos,number);
}