#include <stdio.h>
int a,b,difference;
int main ()
{
    printf("Donnez la valeur de a :");
    scanf("%d", &a);
    printf("Donnez la valeur de b :");
    scanf("%d", &b);
    difference = a-b;
    printf("la difference est : %d", difference);
}
